export const setToken = (x, y) =>{
    sessionStorage.setItem(x, y)
};

export const isLoggedIn = (a, b) =>{
    sessionStorage.setItem(a, b)
};

export const userName = (x, y) =>{
    sessionStorage.setItem(x, y)
};

export const email = (x, y) =>{
    sessionStorage.setItem(x,y)
};

export default setToken